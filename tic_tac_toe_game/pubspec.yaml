name: tic_tac_toe_game
description: A classic TicTacToe game built with Flutter.
publish_to: 'none' # This is a local project, so do not publish.
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
